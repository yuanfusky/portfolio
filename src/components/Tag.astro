---
interface Props { label: string; }
const { label } = Astro.props;
---

<span class="inline-flex rounded-full border border-line bg-surface/70 px-3 py-1 font-mono text-[0.65rem] tracking-wide text-muted">{label}</span>
